version: "3"
services: 
    nginx:
        ports: 
            - "8080:80"
    node-app:
        build: 
            context: . 
            args:
                NODE_ENV: production
        env_file: 
            - ../.env
        command:    node server.js
    